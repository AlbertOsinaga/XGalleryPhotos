<?xml version="1.0" encoding="utf-8"?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms" 
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml" 
             xmlns:vm="clr-namespace:MultiMediaPickerSample.ViewModels" 
             x:Class="XGaleryPhotos.MainPage"
             xmlns:flv="clr-namespace:DLToolkit.Forms.Controls;assembly=DLToolkit.Forms.Controls.FlowListView"
             xmlns:ffimageloading="clr-namespace:FFImageLoading.Forms;assembly=FFImageLoading.Forms"
             xmlns:ios="clr-namespace:Xamarin.Forms.PlatformConfiguration.iOSSpecific;assembly=Xamarin.Forms.Core" 
             xmlns:local="clr-namespace:XGaleryPhotos.Behaviors"
             ios:Page.UseSafeArea="true">

    <ContentPage.Content>
         <StackLayout Padding="10">
            <StackLayout Orientation="Vertical" VerticalOptions="Center" Padding="5">
                <Grid>
                    <Grid.RowDefinitions >
                        <RowDefinition Height="Auto" />
                    </Grid.RowDefinitions>
                    <Grid.ColumnDefinitions >
                        <ColumnDefinition Width="Auto" />
                        <ColumnDefinition Width="100" />
                        <ColumnDefinition Width="100" />
                    </Grid.ColumnDefinitions>
                    <Label Grid.Column="0" Text="Nro. Flujo:" FontSize="Small" VerticalOptions="Center" />
                    <Entry Grid.Column="1" x:Name="txtNroFlujo" Text="24324R" FontSize="Small" VerticalOptions="Center"  />
                    <Button Grid.Column="2" HorizontalOptions="End" Text="Buscar Flujo" TextColor="White" BackgroundColor="Blue"  
                        Command="{Binding BuscarFlujoCommand}"/> 
                </Grid>
                <Grid>
                    <Grid.RowDefinitions >
                        <RowDefinition Height="Auto" />
                    </Grid.RowDefinitions>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="Auto" />
                        <ColumnDefinition Width="Auto" />
                        <ColumnDefinition Width="Auto" />
                        <ColumnDefinition Width="Auto" />
                    </Grid.ColumnDefinitions>
                    <Label Text="Cliente:" FontSize="Small" Grid.Column="0" />
                    <Label Text="Juan Perez" FontSize="Small" Grid.Column="1" FontAttributes = "Bold"/>
                    <Label Text="Placa:" FontSize="Small" Grid.Column="2" />
                    <Label Text="122KTR" FontSize="Small" Grid.Column="3" FontAttributes = "Bold"/>
                </Grid>
                <Grid>
                    <Grid.RowDefinitions >
                        <RowDefinition Height="Auto" />
                    </Grid.RowDefinitions>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="Auto" />
                        <ColumnDefinition Width="*" />
                        <ColumnDefinition Width="Auto" />
                        <ColumnDefinition Width="30" />
                        <ColumnDefinition Width="Auto" />
                    </Grid.ColumnDefinitions>
                    <Label Grid.Column="0" Text="Tipo Documental:" FontSize="Small" VerticalOptions="Center" />
                    <Picker Grid.Column="1" x:Name="pckTipoDocumental" FontSize="Small"
                            SelectedIndexChanged="pckTipoDocumental_SelectedIndexChanged"/>
                    <Label Grid.Column="2" x:Name="lblNumero" Text="Nro:" FontSize="Small" IsVisible="false"
                           VerticalOptions="Center"/>
                    <Entry Grid.Column="3" x:Name="txtNumero" Text="1" FontSize="Small" IsVisible="false" >
                            <Entry.Behaviors>
                                <local:NumericValidationBehavior />
                            </Entry.Behaviors>
                    </Entry>
                    <Label Grid.Column="4" x:Name="lblNumeroLimites" Text="(1 a 20)" FontSize="Micro" VerticalOptions="Center" IsVisible="false" />
                </Grid>
        </StackLayout>
            
            <StackLayout Orientation="Horizontal" >
                <Button TextColor="White" BackgroundColor="Blue" Text="Fotos Galería" 
                        Command="{Binding SelectImagesCommand}"/>
                <Button TextColor="White" BackgroundColor="Blue" Text="Tomar Foto" 
                        Clicked="btnTomarFoto_Clicked"/>
                <Button TextColor="White" BackgroundColor="Blue" Text="Enviar" 
                        Clicked="btnTomarFoto_Clicked"/>
            </StackLayout>     
            
            <flv:FlowListView FlowColumnCount="3" x:Name="listItems" FlowItemsSource="{Binding Media}"
                        SeparatorVisibility="Default"
                        HasUnevenRows="false" RowHeight="120"
                        FlowItemTappedCommand="{Binding PhotoTappedCommand}" >
                    <flv:FlowListView.FlowColumnTemplate>
                        <DataTemplate >
                            <Grid>
                               <ffimageloading:CachedImage  DownsampleToViewSize="true" HeightRequest="100"  
                                                            Source="{Binding PreviewPath}"  
                                                            Aspect="AspectFill" 
                                                            HorizontalOptions="FillAndExpand">
                               </ffimageloading:CachedImage>
                               <Image Source="play" IsVisible="false" HorizontalOptions="End" VerticalOptions="End">
                                   <Image.Triggers>
                                        <DataTrigger TargetType="Image" Binding="{Binding Type}" Value="Video">
                                             <Setter Property="IsVisible" Value="True" />
                                        </DataTrigger>
                                    </Image.Triggers>
                               </Image>
                            </Grid>
                        </DataTemplate>
                    </flv:FlowListView.FlowColumnTemplate>
            </flv:FlowListView>
        </StackLayout>
    </ContentPage.Content>
  
</ContentPage>